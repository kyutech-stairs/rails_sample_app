<h1>Tweets#new</h1>

<%
=begin%>
 <%= form_with(model: @tweet, local: true) do |form| %>
  <div>
    <h3><%= form.label :body %></h3>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %> 
<%
=end%>

<%= render "form", tweet: @tweet %>
